# Personal cross platform dotfiles repo. 

## Use tips:
- Add the '--recurse-submodules' flag to the clone command to automatically initialize and update each submodule in the repository.
- Create symlinks where necessary.
